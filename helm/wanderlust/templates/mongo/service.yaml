apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mongo.name }}-service
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    app: {{ .Values.mongo.name }}
  ports:
    - port: {{ .Values.mongo.port.service }}
      targetPort: {{ .Values.mongo.port.container }}
      protocol: TCP